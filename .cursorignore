# .cursorignore Template
# Controls which directories and files Cursor can access
# Cursor automatically ignores files in `.gitignore` and the default ignore list
# For more info about Cursor Ignore Files, see https://docs.cursor.com/en/context/ignore-files
